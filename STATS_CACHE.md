# 报名统计缓存功能说明

## 功能概述

系统在上传 Excel 职位表时，会自动并发获取所有职位的实时报名统计信息，并将合并后的完整数据缓存到服务器。

## 工作流程

### 1. 上传时自动获取统计

当用户上传 Excel 文件时：

1. **解析 Excel**：系统首先解析 Excel 文件，提取所有职位信息
2. **并发获取统计**：自动并发调用外部 API 获取每个职位的报名统计
   - 并发数：10 个请求同时进行
   - 超时时间：每个请求 10 秒
   - 进度显示：控制台输出实时进度
3. **数据合并**：将报名统计数据合并到职位数据中
   - `报考人数`：当前报考人数
   - `竞争比例`：报考人数 / 招录人数
   - `统计获取成功`：标记该职位统计是否获取成功
4. **服务器缓存**：将完整数据保存到服务器存储

### 2. 数据展示

在职位列表页面：

- **报考人数列**：显示实时报考人数，支持排序
- **竞争比例列**：显示竞争比例，带颜色标识
  - 绿色：竞争比例 < 20:1
  - 橙色：20:1 ≤ 竞争比例 ≤ 50:1
  - 红色：竞争比例 > 50:1

### 3. 手动刷新统计

在数据概览页面，提供"刷新统计"按钮：

- 点击后重新获取所有职位的最新报名统计
- 更新服务器缓存
- 自动刷新页面数据

## API 端点

### POST /api/upload

上传 Excel 文件并自动获取统计

**请求**：
- Content-Type: multipart/form-data
- Body: Excel 文件

**响应**：
```json
{
  "success": true,
  "message": "成功解析 XXX 条职位数据，已获取报名统计",
  "filename": "职位表.xlsx",
  "count": 1234,
  "statsSuccessCount": 1200,
  "statsUpdateTime": "2026-01-23T10:30:00.000Z"
}
```

### POST /api/refresh-stats

手动刷新所有职位的报名统计

**响应**：
```json
{
  "success": true,
  "message": "成功刷新 XXX 条职位的报名统计",
  "count": 1234,
  "statsSuccessCount": 1200,
  "statsUpdateTime": "2026-01-23T11:00:00.000Z"
}
```

### GET /api/positions

获取职位数据（包含缓存的统计信息）

**响应**：
```json
{
  "success": true,
  "metadata": {
    "filename": "职位表.xlsx",
    "uploadTime": "2026-01-23T10:00:00.000Z",
    "count": 1234,
    "source": "uploaded",
    "statsUpdateTime": "2026-01-23T10:30:00.000Z",
    "statsSuccessCount": 1200
  },
  "data": [
    {
      "序号": 1,
      "职位代码": "202601001",
      "职位名称": "警察",
      "招录人数": 2,
      "报考人数": 45,
      "竞争比例": 22.5,
      "统计获取成功": true,
      // ... 其他字段
    }
  ]
}
```

## 数据结构

### PositionData 接口

```typescript
interface PositionData {
  // ... 原有字段
  报考人数?: number          // 当前报考人数
  竞争比例?: number          // 竞争比例（报考人数/招录人数）
  统计获取成功?: boolean     // 统计是否获取成功
}
```

### DataMetadata 接口

```typescript
interface DataMetadata {
  filename: string
  uploadTime: string
  count: number
  source: 'none' | 'uploaded'
  statsUpdateTime?: string      // 统计更新时间
  statsSuccessCount?: number    // 成功获取统计的职位数
}
```

## 性能优化

1. **并发控制**：限制同时请求数为 10，避免服务器压力过大
2. **超时处理**：每个请求 10 秒超时，避免长时间等待
3. **错误容错**：单个职位统计失败不影响整体流程
4. **服务器缓存**：数据缓存在服务器，避免重复请求
5. **进度显示**：控制台实时显示获取进度

## 使用建议

1. **首次上传**：上传 Excel 后等待统计获取完成（可能需要几分钟）
2. **定期刷新**：建议每天刷新一次统计数据，获取最新报考情况
3. **查看详情**：点击"查看详情"按钮可查看单个职位的实时统计
4. **数据分析**：使用排序和筛选功能分析竞争情况

## 注意事项

- 统计数据来自外部 API，可能存在延迟或不准确
- 网络问题可能导致部分职位统计获取失败
- 刷新统计会重新请求所有职位，请勿频繁操作
- 服务器缓存会在重启后保留（使用 Nitro 存储系统）
